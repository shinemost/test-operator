---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: myproxies.gateway.shinemost.top
spec:
  group: gateway.shinemost.top
  names:
    kind: MyProxy
    listKind: MyProxyList
    plural: myproxies
    singular: myproxy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MyProxy is the Schema for the myproxies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MyProxySpec defines the desired state of MyProxy
            properties:
              name:
                description: Name
                type: string
            type: object
          status:
            description: MyProxyStatus defines the observed state of MyProxy
            properties:
              pod_names:
                description: pod名称
                items:
                  type: string
                type: array
            required:
            - pod_names
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
